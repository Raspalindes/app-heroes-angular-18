/**
 * Enum que define las rutas disponibles para la navegación de héroes.
 */
export enum HeroRoutes {
  /** Ruta al listado de héroes */
  LIST = '/heroes/list',
  /** Ruta al formulario de héroes (base) */
  FORM = '/heroes/form',
}
